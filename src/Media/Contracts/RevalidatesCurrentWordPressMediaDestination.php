<?php

namespace hexa_package_wordpress\Media\Contracts;

/**
 * Marks destinations that must re-run provider validation when the selected
 * attachment is already assigned.
 */
interface RevalidatesCurrentWordPressMediaDestination
{
}
